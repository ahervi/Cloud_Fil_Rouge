image: registry.cloud.rennes.enst-bretagne.fr:5000/ws_12


services:
  - mongo
tag:
  script: "cd src/ms/tag; pytest -v"
  stage: test
photo:
  script: "cd src/ms/photo; pytest -v"
  stage: test
photographer:
  script: "cd src/ms/photographer; pytest -v"
  stage: test


